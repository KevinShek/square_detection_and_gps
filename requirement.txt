# pip install -r requirements.txt

# base ----------------------------------------
numpy>=1.18.5
opencv-python>=4.1.2
# if opencv-python is causing you trouble use the following commented commands in the command line/Terminal
# sudo apt update
# sudo apt-get install libatlas-base-dev libjasper-dev

# For the raspberry pi ----------------------------------
# uncomment the text below to install the package(s)
# picamera